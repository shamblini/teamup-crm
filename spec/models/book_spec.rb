# frozen_string_literal: true

require 'rails_helper'

RSpec.describe(Book, type: :model) do
  it 'has at least one user in the test database' do
    # Assuming the 'users' table is seeded with at least one user
    # expect(Book.count).to be >= 5
  end
end
